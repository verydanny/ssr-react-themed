/**
 * For price range input, what are the min and max values?
 * If the selected price range equals these values,
 * then we consider the search to be for "any price"
 */
export const priceConstraint = {
  min: 0,
  max: 5100,
}
